import { BackgroundEstadoDirective } from './background-estado.directive';

describe('BackgroundEstadoDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundEstadoDirective();
    expect(directive).toBeTruthy();
  });
});
