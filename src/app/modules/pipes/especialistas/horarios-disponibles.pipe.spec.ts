import { HorariosDisponiblesPipe } from './horarios-disponibles.pipe';

describe('HorariosDisponiblesPipe', () => {
  it('create an instance', () => {
    const pipe = new HorariosDisponiblesPipe();
    expect(pipe).toBeTruthy();
  });
});
