import { EspecialidadesToStringPipe } from './especialidades-to-string.pipe';

describe('EspecialidadesToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new EspecialidadesToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
